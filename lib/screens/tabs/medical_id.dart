import 'package:flutter/material.dart';

class MedicalID extends StatelessWidget {
  const MedicalID({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}